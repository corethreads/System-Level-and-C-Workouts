%d = show integers

data types : {
    int

  }

GCC -> Gnu c Compiler but now it it stands for  GNU  Compiler Collection cause it runs code for multiple languages.

C is a low level programming language that is used for system programming and embedded systems. It is a procedural programming language that is widely used for developing operating systems, compilers, and other system software. --> but for my case system-level backend engineering.

Compiler --> GCC {
  -commands
      -Wall: show all warnings if there is any
      -o: output file
}


variables.
  -- types of variables
    int : integer numbers
    float : decimal numbers
    char : single characters
    double : double precision decimal numbers
    void : no value

  -- declaration of variables
      int a;
      float weight; etc

  -- assignment of variables
      a = 10;
      weight = 65.5; etc
  -- initialization of variables
      int a = 10;
      float weight = 65.5; etc
